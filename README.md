# SubscriptionTracker (Avalonia) — Tabs + Appointments

Tabs:
- Members: add/search/update members (unique name case-insensitive)
- Reports: Payment Due / All, Pay Selected
- Appointments: add appointment and show **today's** appointments with Due/OK status

DB: `subscriptions.db` in the folder where you run `dotnet run`.

Run:
- `dotnet restore`
- `dotnet run`
